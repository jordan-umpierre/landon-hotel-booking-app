# Landon Hotel Frontend

Angular frontend for the Landon Hotel Booking App.

## Common Commands

- `npm ci` installs frontend dependencies.
- `npm run build` creates the production frontend bundle.
- `npm run test:ci` runs the headless Karma test suite with automatic browser detection.

The repository root README contains the primary local-run and packaging instructions for the full Spring Boot plus Angular application.
